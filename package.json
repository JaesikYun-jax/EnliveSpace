{
  "name": "enlivespace-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "optimize": "node scripts/optimize-images.mjs",
    "build": "node scripts/build-pages.mjs",
    "favicons": "node scripts/build-favicons.mjs",
    "test": "node scripts/test-site.mjs",
    "all": "npm run optimize && npm run build && npm run favicons && npm run test"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}
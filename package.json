{
  "name": "enlivespace-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "stage": "node scripts/stage-images.mjs",
    "optimize": "node scripts/optimize-images.mjs",
    "build": "node scripts/build-pages.mjs",
    "favicons": "node scripts/build-favicons.mjs",
    "portfolio-hero": "node scripts/build-portfolio-hero.mjs",
    "test": "node scripts/test-site.mjs",
    "all": "npm run stage && npm run optimize && npm run portfolio-hero && npm run build && npm run favicons && npm run test"
  },
  "dependencies": {
    "sharp": "^0.34.5"
  }
}